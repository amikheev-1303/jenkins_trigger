node {
    properties([
            parameters([
                    string(name: 'proj', defaultValue: ''),
                    string(name: 'pipe', defaultValue: ''),

            ])
    ])


    stage('test') {
        echo "${proj}"
        echo "${pipe}"
    }
}
