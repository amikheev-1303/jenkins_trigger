node {
    properties([
            parameters([
                    string(name: 'proj', defaultValue: 'Default'),
                    string(name: 'pipe', defaultValue: 'qe pipeline'),

            ])
    ])


    stage('test') {
        echo "${proj}"
        echo "${pipe}"
    }
}
